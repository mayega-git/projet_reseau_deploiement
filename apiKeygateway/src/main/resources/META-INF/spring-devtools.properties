# Spring DevTools Configuration
# Prevents ClassLoader conflicts with Kafka consumers

# Exclude Kafka-related packages from restart
restart.exclude.kafka=com\.example\.newsletter_service\.kafka\..*
restart.exclude.dto=com\.example\.newsletter_service\.dto\..*
restart.exclude.services=com\.example\.newsletter_service\.services\.KafkaConsumerService
